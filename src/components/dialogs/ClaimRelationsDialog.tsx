// ClaimRelationsDialog — removed as part of Graph RAG → Document RAG migration.
// Claim relations were part of the old knowledge graph system.
// The new RAG system uses document-level semantic search instead.

export function ClaimRelationsDialog() {
  return null;
}
